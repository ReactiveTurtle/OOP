#pragma once
#include <vector>

float GetMin(std::vector<float> numbers, bool& wasFound);
std::vector<float> Multiply(std::vector<float> numbers, float number);
std::vector<float> Sort(std::vector<float> numbers);
